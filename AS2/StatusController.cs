using System;

namespace AS2
{
    public class StatusController
    {
        public Status IsNewbie(DateTime startDate)
        {
            var studentTimeSpan = (Datetime.Now.Subtract(startDate)
            if (studentTimeSpan < 60 && studentTimeSpan > 0) => Status.Active;
            

        }

    }
}